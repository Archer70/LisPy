; Application configuration module
(define app-name "LisPy Demo")
(define version "1.0.0")
(define debug-mode true)
(define max-connections 100)

; Private helper (not exported)
(define format-version (fn [v] v))

; Export public configuration
(export app-name version debug-mode max-connections) 